// print one number at a time to the DOM

 // arr = [1, 2, 3, 4, 5]
    // loop through the array
    // print the first number
    // pop it off the front of the array
    // print the first number
    // if there are no more numbers, print "You have reached your savings goal!"
function singleNumber(x){
    const arr = [1, 2, 3, 4, 5]
    for(i = arr[0]; i <= arr.length; i++){
        console.log(arr[0])
    }
}

singleNumber(1)